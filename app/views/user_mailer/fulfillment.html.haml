%h1= @subject

%p Your order for:

%ul
  - @response.orders.each do |order|
    %li= "#{order.supply.name}"

- if @response.orders.any? &:denied?
  %p has been denied. Please see the attached response:
- else
  %p has successfully been fulfilled. Please follow the attached instructions:

%p= @response.extra_text
