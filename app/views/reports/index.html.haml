%h2 Reports

%p Click the report button to download a .csv file

%p= link_to 'Order History', order_history_reports_path(format: :csv), class: "btn"
- if current_user.admin?
  %p= link_to 'Users', users_reports_path(format: :csv), class: "btn"

