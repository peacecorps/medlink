%h1 Add User
= render partial: 'form', locals: {user: @user, action: :add}

%h1 Edit Account
= form_tag '', method: :get do
  = select_tag :edit_user, grouped_options_for_select(@users)
  %button.btn.btn--primary(type="submit") Edit
