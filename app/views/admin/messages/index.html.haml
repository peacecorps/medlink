= title do
  Bulk Messaging

= form_for @bulk, url: admin_messages_path do |f|
  .form-group
    = f.text_field :body, class: "form-control", placeholder: "Type a message", maxlength: 320
  .form-group
    = f.select :country_ids, @bulk.country_options, {}, multiple: true, class: "form-control"
    .help-block Note: standard text rates apply
  %button.btn.btn-primary Send

= title do
  Recent Messages

.row
  .col-md-6
    %h3 Incoming
    = render partial: 'table', locals: { messages: @incoming }

  .col-md-6
    %h3 Outgoing
    = render partial: 'table', locals: { messages: @outgoing }
