%header.l-sidebar
  .l-sidebar__inner
    %img#brand(src="/logo.png")
    %hgroup
      %h1 Peace Corps
      %h4 Medical Supplies
      - if current_user
        %span.badge= current_user.country.name.titleize